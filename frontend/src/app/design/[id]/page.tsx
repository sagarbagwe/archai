"use client";
import { useEffect,useMemo,useRef,useState } from "react";
import { useParams } from "next/navigation";
import { Calculator,Check,Circle,LoaderCircle,Sparkles,X } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { ArchitectureCanvas } from "@/components/architecture-canvas";
import { DesignToolsPanel } from "@/components/design-tools-panel";
import { streamDesignGeneration } from "@/lib/api";
import { buildInitialDiagram } from "@/lib/diagram";
import { DiagramState,GenerationEvent,SavedDesign } from "@/lib/domain/design";
import { browserStorage } from "@/lib/storage/storage-service";
const stages=[["requirements","Analyzing requirements"],["estimation","Calculating traffic"],["architecture","Designing architecture"]] as const;type StageState="pending"|"active"|"complete"|"failed";
export default function DesignPage(){const{id}=useParams<{id:string}>();const[design,setDesign]=useState<SavedDesign>();const[progress,setProgress]=useState<Record<string,StageState>>({});const[generating,setGenerating]=useState(false);const[error,setError]=useState<string>();const controller=useRef<AbortController>();useEffect(()=>setDesign(browserStorage().getDesign(id)),[id]);useEffect(()=>()=>controller.current?.abort(),[]);const metrics=useMemo(()=>design?[["Daily requests",design.estimate?.dailyRequests],["Average RPS",design.estimate?.averageRps],["Peak RPS",design.estimate?.peakRps],["Target latency",`${design.input.expectedLatencyMs} ms`]]:[],[design]);function receive(e:GenerationEvent){if(e.type==="stage.started")setProgress(x=>({...x,[e.stage]:"active"}));if(e.type==="stage.completed")setProgress(x=>({...x,[e.stage]:"complete"}));if(e.type==="stage.failed")setProgress(x=>({...x,[e.stage]:"failed"}))}async function generate(){if(!design||generating)return;setGenerating(true);setError(undefined);setProgress({});controller.current=new AbortController();try{const generatedDesign=await streamDesignGeneration(design.input,receive,controller.current.signal);const updated:SavedDesign={...design,status:"generated",updatedAt:new Date().toISOString(),generatedDesign,diagram:buildInitialDiagram(generatedDesign.architecture)};browserStorage().saveVersion(design,"Before AI generation");browserStorage().saveDesign(updated);setDesign(updated)}catch(c){if((c as Error).name!=="AbortError")setError(c instanceof Error?c.message:"AI generation failed.")}finally{setGenerating(false)}}function saveDiagram(diagram:DiagramState){if(!design)return;browserStorage().saveVersion(design,"Before diagram edit");const updated={...design,diagram,updatedAt:new Date().toISOString()};browserStorage().saveDesign(updated);setDesign(updated)}if(!design)return <AppShell><main className="mx-auto max-w-5xl px-6 py-16"><p className="text-[var(--muted)]">Loading local design…</p></main></AppShell>;const architecture=design.generatedDesign?.architecture;return <AppShell><main className="mx-auto min-h-screen max-w-7xl px-6 py-8"><header className="flex flex-col justify-between gap-5 border-b border-[var(--border)] pb-8 md:flex-row md:items-end"><div><span className="text-sm font-semibold text-[var(--accent)]">{design.status==="generated"?"Generated design":"Local draft"}</span><h1 className="my-2 text-4xl tracking-tight">{design.input.systemName}</h1><p className="m-0 max-w-2xl text-[var(--muted)]">{design.input.problemDescription}</p></div><div className="flex gap-2">{generating&&<button onClick={()=>controller.current?.abort()} className="flex min-h-12 items-center gap-2 rounded-lg border border-[var(--border)] px-4"><X size={17}/>Cancel</button>}<button onClick={generate} disabled={generating} className="flex min-h-12 items-center gap-2 rounded-lg bg-[var(--accent)] px-5 font-semibold text-white disabled:opacity-60">{generating?<><LoaderCircle className="animate-spin" size={18}/>Generating</>:<><Sparkles size={18}/>{design.status==="generated"?"Regenerate":"Generate architecture"}</>}</button></div></header>{generating&&<section className="my-6 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-5" aria-live="polite"><h2 className="mt-0 text-base">Building your system design</h2><div className="grid gap-3 md:grid-cols-3">{stages.map(([key,label])=>{const s=progress[key]??"pending";return <div key={key} className="flex items-center gap-3 rounded-lg border border-[var(--border)] bg-[var(--canvas)] p-4">{s==="complete"?<Check className="text-green-600"/>:s==="active"?<LoaderCircle className="animate-spin text-[var(--accent)]"/>:s==="failed"?<X className="text-red-600"/>:<Circle className="text-[var(--muted)]"/>}<span className="text-sm font-semibold">{label}</span></div>})}</div></section>}{error&&<p role="alert" className="my-6 rounded-lg border border-red-300 bg-red-50 p-4 text-red-700">{error}</p>}<section className="grid gap-4 py-8 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([label,value])=><div key={label} className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-5"><Calculator size={18} className="text-[var(--accent)]"/><p className="mb-1 mt-5 text-sm text-[var(--muted)]">{label}</p><b className="text-2xl">{typeof value==="number"?value.toLocaleString():value}</b></div>)}</section>{architecture&&design.diagram?<><ArchitectureCanvas key={design.generatedDesign?.title} diagram={design.diagram} onSave={saveDiagram}/><DesignToolsPanel design={design}/></>:<section className="rounded-xl border border-dashed border-[var(--border)] p-12 text-center"><h2>Architecture not generated yet</h2><p className="text-[var(--muted)]">Generate the design to open the interactive workspace.</p></section>}<section className="mt-8 rounded-xl border border-[var(--border)] p-6"><h2 className="mt-0">Calculation formulas</h2><div className="grid gap-3 md:grid-cols-2">{Object.entries(design.estimate?.formulas??{}).map(([name,formula])=><div key={name} className="rounded-lg bg-[var(--surface)] p-4"><b className="text-sm">{name}</b><code className="mt-2 block text-sm text-[var(--muted)]">{formula}</code></div>)}</div></section></main></AppShell>}
