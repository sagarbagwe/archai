"use client";
import { useState } from "react";
import { AppShell } from "@/components/app-shell";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8080";
export default function Interview(){const[system,setSystem]=useState("Global Payment Platform");const[difficulty,setDifficulty]=useState("Advanced");const[question,setQuestion]=useState("");const[answer,setAnswer]=useState("");const[feedback,setFeedback]=useState<{score:number;feedback:string;nextQuestion:string}>();const[step,setStep]=useState(1);async function start(){const r=await fetch(`${API}/api/interview/start`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({systemName:system,difficulty})});const data=await r.json();setQuestion(data.question);setFeedback(undefined)}async function evaluate(){const r=await fetch(`${API}/api/interview/evaluate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({systemName:system,difficulty,answer,step})});const data=await r.json();setFeedback(data);setStep(data.step)}return <AppShell><main className="mx-auto max-w-4xl p-6 py-10"><p className="m-0 text-sm font-semibold text-[var(--accent)]">Practice room</p><h1 className="my-2 text-4xl tracking-tight">System design interview</h1><p className="text-[var(--muted)]">Clarify, estimate, design, defend trade-offs, and improve with structured feedback.</p><section className="mt-8 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">System<input value={system} onChange={e=>setSystem(e.target.value)} className="mt-2 min-h-11 w-full rounded-lg border border-[var(--border)] bg-[var(--canvas)] px-3"/></label><label className="text-sm font-semibold">Difficulty<select value={difficulty} onChange={e=>setDifficulty(e.target.value)} className="mt-2 min-h-11 w-full rounded-lg border border-[var(--border)] bg-[var(--canvas)] px-3">{["Beginner","Intermediate","Advanced","FAANG-level"].map(x=><option key={x}>{x}</option>)}</select></label></div><button onClick={start} className="mt-5 min-h-11 rounded-lg bg-[var(--accent)] px-5 font-semibold text-white">Start interview</button></section>{question&&<section className="mt-5 rounded-xl border border-[var(--border)] p-6"><p className="text-xs font-semibold text-[var(--accent)]">QUESTION {step}</p><h2>{question}</h2><textarea value={answer} onChange={e=>setAnswer(e.target.value)} placeholder="Think aloud. State assumptions and show calculations…" className="min-h-48 w-full rounded-lg border border-[var(--border)] bg-[var(--surface)] p-4"/><button onClick={evaluate} disabled={!answer.trim()} className="mt-4 min-h-11 rounded-lg bg-[var(--text)] px-5 font-semibold text-[var(--canvas)] disabled:opacity-50">Evaluate answer</button>{feedback&&<div className="mt-5 rounded-lg bg-[var(--accent-soft)] p-4"><b className="text-[var(--accent)]">Score {feedback.score}/10</b><p>{feedback.feedback}</p><button onClick={()=>{setQuestion(feedback.nextQuestion);setAnswer("");setFeedback(undefined)}} className="font-semibold text-[var(--accent)]">Next question →</button></div>}</section>}</main></AppShell>}
